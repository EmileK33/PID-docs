"""Service layer (business logic between routers and the DB/storage layers).

Phase-2 sessions add one module per feature here. S2-D owns the export service
(``export_service``) and the file generators (``export_generators``).
"""
